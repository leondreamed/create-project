{
	"name": "@{{project_name}}/monorepo",
	"scripts": {
		"prepare": "lion-git-hooks",
		"build": "pnpm recursive run build"
	},
	"devDependencies": {
		"lionconfig": "latest",
		"@types/node": "latest",
		"typescript": "latest"
	}
}
